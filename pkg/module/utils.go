package modules

import (
	"github.com/bacalhau-project/lilypad/pkg/data"
)

// get a module cloned and checkedout then return the
// text content of the template
func prepareModule(module data.Module) (string, error) {
	return "", nil
}
